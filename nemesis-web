#!/usr/bin/env bash
# Lance l'interface web NEMESIS (moteur CLI + FastAPI).
# Usage : ./nemesis-web [--host 0.0.0.0] [--port 3080]
cd "$(dirname "$(readlink -f "$0")")" || exit 1
exec python3 -m web.server "$@"
