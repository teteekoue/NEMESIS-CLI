calculator:
  command: python3 mcp_calculator.py
  description: Built-in MCP calculator (math operations)

# Example (uncomment and set token via env — do not commit secrets):
# github:
#   command: npx -y @modelcontextprotocol/server-github
#   env:
#     GITHUB_PERSONAL_ACCESS_TOKEN: "YOUR_TOKEN_HERE"
